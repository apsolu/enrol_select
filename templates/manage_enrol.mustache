<form method="post" action="{{ action }}">
    <div class="enrol-select-manage-fieldset-div">
        <label for="addselect">{{# str}}users, moodle{{/ str}}</label>
        {{{ potential_users_selector }}}
    </div>

    <dl id="adduseroptionsdl" class="enrol-select-manage-fieldset-div">
        <div>
            <dt>Rôle</dt>
            <dd>
                <select name="role">
                    {{# roles}}
                    <option value="{{ id }}">{{ name }}</option>
                    {{/ roles}}
                </select>
            </dd>
        </div>
        <div>
            <dt>Liste</dt>
            <dd>
                <select name="status">
                    {{# status}}
                    <option value="{{ id }}">{{ name }}</option>
                    {{/ status}}
                </select>
            </dd>
        </div>
    </dl>

    <p>
        <input class="btn btn-primary" type="submit" value="Inscrire les utilisateurs" />
    </p>
</form>
